# Создание документации dbt

dbt позволяет автоматически генерировать документацию для моделей на основе YAML-файлов. Команда `dbt docs` компилирует описания моделей и запускает веб-сервер для просмотра.

В этом упражнении мы не будем просматривать документацию в браузере, а только убедимся, что она генерируется корректно.

## Задание

Добавьте описание модели в `model_properties.yml`, сгенерируйте документацию и запустите сервер.

<details>
<summary>Решение</summary>

Замените <code>_____</code> на <code>Первичный импорт данных NYC Yellow Taxi из Parquet-источника</code>, затем <code>dbt docs generate</code> и <code>dbt docs serve</code>. Остановите сервер через <code>Ctrl+C</code>.

</details>
